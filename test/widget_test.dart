import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Sample placeholder test', (WidgetTester tester) async {
    // Tes ini adalah placeholder dan akan selalu berhasil.
    // Ini memastikan file tes valid dan tidak menyebabkan error.
    expect(1, 1);
  });
}